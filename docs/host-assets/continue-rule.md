# Continue Rule Template

Suggested Continue rule content:

```markdown
Use GrokSearch as the default MCP-backed web context tool.

- Start with `plan_* -> web_search` for most research tasks
- Call `get_sources` for citation checks
- Use `web_fetch` for a single page and `web_map` for site structure
- Reserve `deep_research_*` for advanced report-style work
- Keep interactive deep research in `grok-search-research`

Optimize for source-backed retrieval, not speculative browsing.
```
